# Core LLM + AI APIs
openai>=1.0.0,<2

# ElevenLabs Python SDK (v2 API — matches text_to_speech.convert usage)
elevenlabs>=2.10.0,<3

# LangChain stack (compatible with Pydantic v2)
pydantic>=2.7,<3
langchain>=0.2.16
langchain-core>=0.2.38
langchain-openai>=0.1.24

# Streamlit frontend
streamlit>=1.27.0
streamlit-mic-recorder==0.0.4
# Resume parsing
pymupdf>=1.22.0            # pip name is lowercase (NOT PyMuPDF)
python-docx>=0.8.11
pytesseract>=0.3.10
Pillow>=10.0.0

# Whisper (local transcription)
openai-whisper             # requires FFmpeg; install torch separately (see below)

# Audio I/O
SpeechRecognition>=3.10.0

# Utilities
numpy>=1.25.2
python-dotenv>=1.0.0

# Optional (for local dev and debug)
tqdm
